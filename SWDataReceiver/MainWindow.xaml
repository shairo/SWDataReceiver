<Window x:Class="SWDataReceiver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWDataReceiver"
        mc:Ignorable="d"
        Title="StormWorks Composite Data Recorder" Height="104" Width="360" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="Record" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,41,0,0" Width="75" Height="20" Command="{Binding StartButtonCommand}"/>
        <Button Content="Stop" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="90,41,0,0" Width="75" Height="20" Command="{Binding StopButtonCommand}"/>
        <Button Content="Clear" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="170,41,0,0" Width="75" Height="20" Command="{Binding ClearButtonCommand}"/>
        <Button Content="Save As" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="250,41,0,0" Width="75" Height="20" Command="{Binding SaveAsButtonCommand}"/>
        <Label Content="{Binding Path=DataCountLabel}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" Width="315" Height="26"/>

    </Grid>
</Window>
